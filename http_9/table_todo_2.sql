CREATE TABLE todo_2 (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT,
    content TEXT,
    public INTEGER,
    food_orange INTEGER,
    food_apple INTEGER,
    food_banana INTEGER,
    pub_date TEXT,
    qty1 INTEGER,
    qty2 INTEGER,
    qty3 INTEGER,
    created_at TEXT NOT NULL
);
